// Stub for `react-devtools-core`, which Ink imports only through a guarded
// dynamic import in development. The standalone bundle aliases the package to
// this empty module so production builds don't pull the dev-only dependency in.
export default {};
